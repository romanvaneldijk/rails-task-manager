<div class= "text-center p-20 border-bottom" style="width: 60%; margin: 0 auto; padding: 50px 0px 0px 0px;">

<h1>My tasks</h1>
<ul class="d-inline text-justify">
<% @tasks.each do |task| %>
<li>
  <% if task.completed?%>
  <i class="far fa-check-square"></i>
  <% else %>
    <i class="far fa-square"></i>
  <% end %>
  <%= link_to task.title, task_path(task) %>
  <%= link_to '🗑', task_path(task), method: :delete, data: {confirm: "Are you sure about that?"} %>
</li>
<% end %>
<li style="margin-top: 20px;"><%= link_to ' + create new task', task_path(:new) %></li>
</ul>

</div>
